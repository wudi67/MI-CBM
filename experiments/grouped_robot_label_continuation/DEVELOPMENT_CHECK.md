# 本次交付的工程检查

默认 300+100→300+300 实验尚未启动。以下结果只说明代码、来源配对和恢复流程通过验证。

## 自动检查

命令：`bash experiments/grouped_robot_label_continuation/scripts/check.sh`。

- Ruff、格式检查、ty、Pylint 全部通过。
- pytest：3 passed，29.38 秒；一个环境原有的 pynvml 弃用警告。
- 两份 label 电路恢复完整 Adam 后，与原训练循环从头连续训练到同一终点的
  模型、Adam 和随机数状态一致。
- 新增的纠正诊断不改变优化轨迹；保留原历史前缀和样本顺序。
- 半个 epoch 中断恢复后，与不中断的续训结果一致。
- 确认续训来自 long_concept 的后半段，而不是原 long_label 的后半段。
- 冻结前半段没有梯度；后半段梯度非零，输入和参数使用 CUDA。
- 验证概念概率不变、预定诊断与终点评价一致、已完成结果复用，以及异常配置、
  样本顺序和结果篡改拒绝。测试 fixture 不含测试集 CSV。

## 默认来源检查

`scripts/run.sh --plan-only` 通过。
另用只读 Reference 加载器完整核对默认原实验，检查源码、数据、图像、
三项原始训练、五项原续训任务、全部原始预测与来源锁。

确认选中的是概念实际训练 300 轮的 `long_concept`：

| 后半段 | 当前 label 轮数 | 实际 Adam 步数 | 原样本排列偏移 |
|---|---:|---:|---:|
| independent | 100 | 1800 | 100 |
| no_feedback | 100 | 1800 | 100 |

数据为训练 18432 张、验证 6144 张。读取来源不写入原输出，不读取测试集。

## 真实 Robot 数据检查

使用 README 中的小规模命令，来源为
`outputs/grouped_robot_continuation/development_check/long_concept/`。
固定该开发来源的概念第 3 轮模型，两个 label 电路分别从第 2 轮续训至第 4 轮，
输入为真实 Robot 训练 1024 张、验证 2048 张；batch=1024、eval batch=2048。

- 两项训练任务完成，共新增 4 次 Adam 更新。
- 16 个验证条件和 6 个训练诊断完成，共 22 条结果。
- 全部概念纠正的曲线包含起点第 2 轮及第 3、4 轮。
- 两项训练的输入及梯度检查均为 `cuda:0`；前半段无梯度，后半段梯度非零。
- 完整核对结果锁中 101 个文件；查看训练、纠正准确率及损失曲线，均通过。
- 标记 `engineering_subset=true`、`test_read=false`、`test_evaluated=false`。
- tmux 运行结束自动退出。

随后用新 tmux 会话执行 `--resume`，完成全部校验后退出，没有新增训练或推理。
除日志、心跳和进程锁外，102 个输出文件的哈希与修改时间均未变化。
原源码、默认基准、原续训结果和开发来源共 511 个文件的哈希及修改时间保持一致。
默认输出目录尚未创建。

小规模输出不能用于判断默认 300 轮实验的性能收益。
